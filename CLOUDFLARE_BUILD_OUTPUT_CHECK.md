# Cloudflare Pages Build Output Directory 확인 방법

## 📍 확인 위치 (단계별)

### Step 1: Cloudflare 대시보드 접속
1. 브라우저에서 접속: `https://dash.cloudflare.com`
2. 로그인

### Step 2: Pages 프로젝트로 이동
1. 왼쪽 사이드바에서 **"Workers & Pages"** 클릭
2. 상단 탭에서 **"Pages"** 탭 선택 (Workers 탭이 아님!)
3. 프로젝트 목록에서 **"rakorean"** (또는 프로젝트 이름) 클릭

### Step 3: Settings 탭 열기
프로젝트 페이지에서:
1. 상단 탭 메뉴 확인:
   ```
   [Deployments] [Settings] [Custom domains] [Analytics] ...
   ```
2. **"Settings"** 탭 클릭

### Step 4: Builds & deployments 섹션 찾기
Settings 페이지에서:
1. 왼쪽 사이드바 또는 페이지 중앙에 다음 섹션들이 보입니다:
   - **Builds & deployments** ← 여기!
   - Environment variables
   - Custom domains
   - Functions
   - ...

2. **"Builds & deployments"** 섹션 클릭

### Step 5: Build output directory 확인
**"Builds & deployments"** 섹션에서 다음 설정들을 확인할 수 있습니다:

```
Framework preset: [None ▼]
Build command: [________________]
Build output directory: [________________]  ← 여기!
Root directory: [________________]
```

**Build output directory** 필드를 확인하세요:
- ✅ 올바른 설정: `.` (점 하나만)
- ❌ 잘못된 설정: `dist`, `build`, `public` 등

---

## 🔍 스크린샷으로 설명

```
Cloudflare Dashboard
└── Workers & Pages
    └── Pages 탭
        └── rakorean 프로젝트 클릭
            └── Settings 탭 클릭
                └── Builds & deployments 섹션
                    └── Build output directory 필드 확인
```

---

## ✏️ 수정 방법

### Build output directory가 `.` (점 하나)가 아닌 경우:

1. **"Build output directory"** 필드 클릭
2. 기존 내용 삭제
3. **`.` (점 하나만)** 입력
4. **"Save"** 버튼 클릭 (페이지 하단 또는 상단)
5. **"Retry build"** 클릭하여 재배포

---

## 📋 전체 빌드 설정 확인 체크리스트

**"Builds & deployments"** 섹션에서 다음을 모두 확인하세요:

- [ ] **Framework preset**: `None` 또는 `Plain HTML`
- [ ] **Build command**: (비워두기 - 아무것도 입력하지 않음)
- [ ] **Build output directory**: `.` (점 하나만)
- [ ] **Root directory**: (비워두기 - 아무것도 입력하지 않음)
- [ ] **Production branch**: `main`

---

## 💡 팁

### 설정이 보이지 않는 경우:
1. 프로젝트가 최근에 생성되었는지 확인
2. 페이지를 새로고침 (F5 또는 Cmd+R)
3. 브라우저 캐시 삭제 후 다시 접속

### 저장 후 변경사항이 적용되지 않는 경우:
1. **"Save"** 버튼을 클릭했는지 확인
2. 저장 후 **"Retry build"** 클릭
3. 배포 로그에서 변경사항이 반영되었는지 확인

---

## 🆘 여전히 찾을 수 없다면

다음 정보를 알려주세요:
1. Cloudflare Pages 프로젝트 페이지에서 보이는 탭 목록
2. Settings 탭에서 보이는 섹션 목록
3. 스크린샷 (가능하면)

이 정보를 주시면 더 정확하게 안내하겠습니다.

