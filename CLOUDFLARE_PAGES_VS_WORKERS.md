# ⚠️ 중요: Cloudflare Pages vs Workers 차이점

## 🔴 현재 상황

현재 **Cloudflare Workers** 설정 페이지를 보고 계십니다.
- "Deploy command: npx wrangler deploy" ← 이것은 Workers 설정입니다
- "Build output directory" 설정이 보이지 않음 ← Pages에만 있는 설정입니다

## ✅ 우리가 필요한 것: Cloudflare Pages

**Cloudflare Pages**를 사용해야 합니다!

---

## 🔄 올바른 서비스로 전환하는 방법

### Step 1: Cloudflare 대시보드로 돌아가기

1. 현재 페이지에서 브라우저의 **뒤로 가기** 버튼 클릭
2. 또는 왼쪽 사이드바에서 **"Workers & Pages"** 클릭

### Step 2: Pages 탭 선택

**중요**: 두 개의 탭이 있습니다:
- ❌ **"Workers"** 탭 (현재 여기에 있음)
- ✅ **"Pages"** 탭 (여기로 이동해야 함)

상단 탭 메뉴에서 **"Pages"** 탭을 클릭하세요.

### Step 3: Pages 프로젝트 생성

**"Pages"** 탭에서:
1. **"Create a project"** 버튼 클릭
2. **"Connect to Git"** 선택
3. GitHub 계정 연결
4. 저장소 선택: `bunz5911/R`
5. **"Begin setup"** 클릭

### Step 4: Pages 프로젝트 설정

**Pages 프로젝트 설정 화면**에서 다음을 입력:

```
Project name: rakorean
Production branch: main

Framework preset: None (또는 Plain HTML)
Build command: (비워두기 - 아무것도 입력하지 않음)
Build output directory: . (점 하나만 입력)  ← 여기서 확인!
Root directory: (비워두기)
```

**⚠️ 중요**: 
- **Build output directory** 필드가 보여야 합니다 (Pages에만 있음)
- Workers에는 이 설정이 없습니다!

---

## 📊 Pages vs Workers 비교

| 항목 | Cloudflare Pages | Cloudflare Workers |
|------|------------------|-------------------|
| 용도 | 정적 HTML 사이트 호스팅 | 서버리스 함수 실행 |
| Build output directory | ✅ 있음 | ❌ 없음 |
| Deploy command | 자동 | `npx wrangler deploy` |
| 우리 프로젝트에 적합 | ✅ 예 | ❌ 아니오 |

---

## 🔍 Pages 설정 화면 확인 방법

**Pages 프로젝트 설정** 화면에서는 다음이 보여야 합니다:

```
Project name: [rakorean]
Production branch: [main]

Framework preset: [None ▼]
Build command: [________________]  ← 비워두기
Build output directory: [.]  ← 점 하나만
Root directory: [________________]  ← 비워두기
```

**"Build output directory"** 필드가 보이면 올바른 Pages 설정입니다!

---

## ✅ 체크리스트

올바른 서비스를 사용하고 있는지 확인:

- [ ] **"Pages"** 탭에서 프로젝트를 만들었는지 확인
- [ ] **"Build output directory"** 필드가 보이는지 확인
- [ ] **"Deploy command"** 필드가 보이지 않는지 확인 (Workers에만 있음)

---

## 🚀 다음 단계

1. **Workers 탭에서 나가기**
2. **Pages 탭으로 이동**
3. **새 프로젝트 생성** (또는 기존 Pages 프로젝트가 있다면 그것 사용)
4. **Build output directory: `.` (점 하나)** 설정 확인
5. **배포**

---

## 💡 팁

현재 Workers 프로젝트가 있다면:
- Workers 프로젝트는 삭제하거나 무시하세요
- Pages 프로젝트를 새로 만들어야 합니다
- 두 서비스는 완전히 별개입니다

